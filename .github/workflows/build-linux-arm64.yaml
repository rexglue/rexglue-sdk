name: Build rexglue-sdk linux ARM64

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: ubuntu-24.04-arm

    env:
      CC: clang-20
      CXX: clang++-20

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Clang 20
        run: |
          sudo apt-get update
          sudo apt-get install -y wget gnupg lsb-release software-properties-common
          wget -O - https://apt.llvm.org/llvm-snapshot.gpg.key | sudo apt-key add -
          sudo add-apt-repository "deb https://apt.llvm.org/noble/ llvm-toolchain-noble-20 main"
          sudo apt-get update
          sudo apt-get install -y clang-20 lld-20

      - name: Update alternatives to use Clang 20
        run: |
          sudo update-alternatives --install /usr/bin/clang clang /usr/bin/clang-20 200
          sudo update-alternatives --install /usr/bin/clang++ clang++ /usr/bin/clang++-20 200
          clang --version
          clang++ --version

      - name: Install build dependencies
        run: |
          sudo apt update
          sudo apt install -y cmake ninja-build build-essential git curl unzip autoconf python3.12-venv \
          libgtk-3-dev libx11-xcb-dev libvulkan-dev libasound2-dev libpulse-dev

      - name: Configure (CMake preset)
        run: cmake --preset linux-arm64

      - name: Make Install
        run: cmake --build out/build/linux-arm64 --target install -j$(nproc)

      - name: Package install directory
        run: |
          cd out/install
          zip -r rexglue-sdk-linux-arm64.zip linux-arm64

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: out/install/rexglue-sdk-linux-arm64.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
